export const MINUTES_TO_SECONDS_RATIO = 60;
export const SECONDS_TO_MILLISECONDS_RATIO = 1000;
export const graceExitCode = 0;
export const HEARTBEAT_START_TIMEOUT_SECONDS = 10;
export const HEARTBEAT_START_TIMEOUT_MS = HEARTBEAT_START_TIMEOUT_SECONDS * SECONDS_TO_MILLISECONDS_RATIO;

// eslint-disable-next-line @typescript-eslint/no-magic-numbers
export const emptyCollectionSize = 0 as const;
